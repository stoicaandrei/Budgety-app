import BaseLayout from "@/layouts/BaseLayout";
import { Card } from "antd";

const Dashboard = () => {
  return (
    <BaseLayout>
      <Card>Dashboard</Card>
    </BaseLayout>
  );
};

export default Dashboard;
